name: journaltrend
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.11.5 <4.0.0'

dependencies:
  fl_chart: ^1.2.0
  flutter:
    sdk: flutter
  go_router: ^17.3.0
  http: ^1.6.0
  intl: ^0.20.2
  provider: ^6.1.5+1
  url_launcher: ^6.3.2
  google_fonts: ^6.2.1
  shimmer: ^3.0.0
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
